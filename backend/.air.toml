# .air.toml
root = "."
tmp_dir = "tmp"

[build]
  bin = "./tmp/main"
  cmd = "go build -o ./tmp/main ./server/main.go"
  delay = 1000
  exclude_dir = ["tmp", ".git", "vendor"]
  include_ext = ["go", "tpl", "tmpl", "html"]
  exclude_file = []

[log]
  color = true

[watcher]
  paths = ["."]